spring:
    session:
        redis:
            namespace: ion:session
        timeout: 168h

    thymeleaf:
        prefix: classpath:/html/

    web:
        resources:
            static-locations: classpath:/static

    datasource:
        url: jdbc:postgresql://localhost:5432/ion
        username: ion
        password: 2309ur02390g9wug
        driver-class-name: org.postgresql.Driver

    data:
        redis:
            host: localhost
            port: 6379
